import { prisma } from '@/lib/db'; import PostCard from '@/components/post-card'; export default async function Home(){ const posts = await prisma.post.findMany({ where:{ status:'PUBLISHED' }, orderBy:{ publishedAt:'desc' }, take:6 }); return (<main className="max-w-5xl mx-auto p-4"><h1 className="text-3xl font-bold">Latest</h1><div className="grid sm:grid-cols-2 gap-4 mt-4">{posts.map(p=>(<PostCard key={p.id} post={p} />))}</div></main>); }
