"use client"; import { useState } from "react"; export default function Login(){ const [u,setU]=useState(''); const [p,setP]=useState(''); async function submit(e){ e.preventDefault(); const res=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({u,p})}); if(res.ok) window.location.href='/admin'; else alert('Invalid'); } return (<form onSubmit={submit} className="max-w-sm mx-auto p-4"><input value={u} onChange={e=>setU(e.target.value)} placeholder="username" className="w-full mb-2"/><input type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="password" className="w-full mb-2"/><button className="btn">Login</button></form>); }
