# ByteTrend package

Admin: bytetrend_admin / Bt$2025_strong!

See env-vars.txt to copy env vars into Vercel.

Deploy: push to GitHub -> Vercel -> Import repo -> set envs -> deploy

After deploy visit /api/seed to create starter posts.
